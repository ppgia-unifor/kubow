sudo: false

language: java

jdk:
  - openjdk11

install: mvn clean install

cache:
  directories:
  - $HOME/.m2


env:
  matrix:
    - ROOT_POM="pom.xml"

branches:
  only:
    - master
    - /^release.*$/