vars:
# @ZNN_HOME will be replaced by the rainbow-install script
#  _effectors.commonPath: "@ZNN_HOME/effectors"
  _effectors.commonPath: "/Users/carlosmendes/unifor/kube-rainbow/src/main/resources/default/system/effectors/"

effector-types:

effectors:

  setFidelity-web0:
    location: "${customize.system.target.web0}"
    command: "${customize.system.target.lb}.setFidelity($<fidelity>)"
    type: script
    scriptInfo:
      path    : "${_effectors.commonPath}/fidelity-rollout.sh"
      argument: "{0}"
      
  setFidelity-web1:
    location: "${customize.system.target.web2}"
    command: "${customize.system.target.lb}.setFidelity($<fidelity>)"
    type: script
    scriptInfo:
      path    : "${_effectors.commonPath}/fidelity-rollout.sh"
      argument: "{0}"

  setFidelity-web2:
    location: "${customize.system.target.web1}"
    command: "${customize.system.target.lb}.setFidelity($<fidelity>)"
    type: script
    scriptInfo:
      path    : "${_effectors.commonPath}/fidelity-rollout.sh"
      argument: "{0}"